# Requirement / 需求

Move the player spawn position to world coordinates `x = 335`, `z = -358`.

将玩家出生点移动到世界坐标 `x = 335`、`z = -358`。

# Summary / 概要

This change updates the configured player spawn coordinate while leaving spawn height handling and all scene systems unchanged.

本次变更仅更新玩家出生点坐标，不改变出生高度处理或任何场景系统。

# User Request / 用户需求

The user asked to change the spawn point to `335, -358`.

用户要求将出生点改到 `335, -358`。

# Scope / 范围

Update only the player spawn position constant and its matching requirement document. Do not modify scene setup, terrain, water systems, leaf decals, enemy assets, or other unrelated local changes.

仅更新玩家出生点常量和对应需求文档。不修改场景设置、地形、水系、落叶贴花、敌人资源或其他无关本地改动。

# Acceptance Criteria / 验收标准

- `PLAYER_SPAWN_POSITION.x` is `335`.
- `PLAYER_SPAWN_POSITION.z` is `-358`.
- Build verification passes.
- No unrelated local files are included in the commit.

- `PLAYER_SPAWN_POSITION.x` 为 `335`。
- `PLAYER_SPAWN_POSITION.z` 为 `-358`。
- 构建验证通过。
- 提交中不包含无关本地文件。
